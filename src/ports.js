const Ports = [new Portfolio("https://google.drive", 2019, "Worlds", "Control")];





const returnUrls = []

const search = (tags) => { 
    for(const i = 0; i < Ports.length(); i++) { 
        for(const i = 0; i < tags.length; i++) { 
            if(Ports[i].getLevel == tags[0] && Ports[i].getLevel == tags[0] && Ports[i].getLevel == tags[0]) { 

            }
        }
    }
}